# 複製這份為 .env,填入你自己的金鑰。.env 已被 .gitignore 排除,不會被提交。
#
# 本機/demo 開發不需要這個檔案 —— 不設定就會自動走 claude.ai 訂閱額度(CLI)。
# 只有要對外上線(讓客戶連線)時才需要,因為個人訂閱不得用於服務外部客戶。

ANTHROPIC_API_KEY=your-api-key-here

# 選填,預設 500(次/天),失控時的安全氣囊,不取代 console.anthropic.com 的計費上限
# MAX_AI_REQUESTS_PER_DAY=500
