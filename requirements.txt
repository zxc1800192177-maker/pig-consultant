# 這個專案原本刻意不用任何第三方套件(純標準庫)。
# 唯一的例外是資料庫驅動 —— 帳號系統需要它,而 Python 標準庫沒有
# Postgres 驅動。除此之外仍然不用框架、不用 ORM,SQL 直接寫在 db.py。
#
# 沒安裝也不會壞:db.py 對 import 失敗做了處理,沒設 DATABASE_URL 時
# 帳號功能整個關閉,其餘功能照常運作。
psycopg[binary]>=3.1
